projection;
strict ( 2 );
use draft;

define behavior for YSAN_C_PO_H //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action confirm_order;

  use action Edit;
  use action Activate;
  use action Resume;
  use action Discard;
  use action Prepare;

  use association _item { create; with draft; }
}

define behavior for YSAN_C_PO_I //alias <alias_name>
{
  use update;
  use delete;

  use association _order { with draft; }
}